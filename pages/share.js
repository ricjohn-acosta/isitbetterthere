import Share from "../containers/Share"
import Layout from "../components/Layout/Layout";

const share = () => {
    return (
        <Layout>
            <Share/>
        </Layout>
    );
};

export default share;